spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_demo?useSSL=false&serverTimezone=UTC
    username: root
    password: Dusansavic#1277
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibrenate:
      ddl-auto: update
      show-sql: true
      properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect